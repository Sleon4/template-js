(function () {
    const out = {
        has_next_page: true,
        wait: true,
        waitFor: "",
    };

    out.pass_it = !pass_it.cont ? { cont: 0 } : pass_it;

    // more ...

    return out;
})();